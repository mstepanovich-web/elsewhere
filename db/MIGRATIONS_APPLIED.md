# Migrations Applied to Production

This file tracks which `db/*.sql` migrations have been applied to the production Supabase database (project `gbrnuxyzrlzbybvcvyzm`). Update this file whenever a migration is run against prod.

**Doctrine (per CLAUDE.md):** A migration committed to the repo but not applied to prod is NOT shipped — only the actual prod application counts. Update this table when applying a migration; flag `❓ Verify` if applied state is unknown.

| Migration | Applied | Date Applied | Applied By | Notes |
|---|---|---|---|---|
| 001_user_management_schema.sql | ✅ | (pre-Session-5) | Mike | Foundational auth + profiles schema. Production sign-in works → applied. |
| 002_contacts_avatars.sql | ✅ | (pre-Session-5) | Mike | Avatar uploads via Supabase Storage verified on hardware (recent contacts work, commits `b267e41` + `5e44d5e`). |
| 003_admin_and_venue_settings.sql | ✅ | (pre-Session-5) | Mike | Venue admin functions used in karaoke/stage.html admin dialog. |
| 004_rename_is_admin_to_is_platform_admin.sql | ✅ | (pre-Session-5) | Mike | Column rename; downstream code references `is_platform_admin` and works. |
| 005_front_back_venue_tuning.sql | ✅ | (pre-Session-5) | Mike | Venue tuning fields (back_yaw, back_pitch) used by karaoke stage admin dialog. |
| 006_household_and_tv_devices.sql | ✅ | (Session 4.10) | Mike | Household + TV claim flow shipped in Session 4.10; production works end-to-end. |
| 007_anon_tv_is_registered.sql | ✅ | (Session 4.10) | Mike | TV registration check used at boot; claim flow works. |
| 008_sessions_and_participants.sql | ✅ | (Session 5 Part 1a) | Mike | Sessions schema verified on hardware in 3a.1 + 3a.2 (manager identity derives from session_participants.control_role). |
| 009_session_lifecycle_rpcs.sql | ✅ | (Session 5 Part 1b.1) | Mike | rpc_session_start/join/leave/end all verified on hardware in 3a.1 + 3a.2. |
| 010_manager_mechanics_rpcs.sql | ✅ | (Session 5 Part 1b.2) | Mike | Auto-promote + reclaim_manager RPCs. Karaoke Part 2e session lifecycle relies on db/010's updated rpc_session_leave; 2e shipped end-to-end. |
| 011_role_and_queue_mutation_rpcs.sql | ✅ | (Session 5 Part 1b.3) | Mike | rpc_session_update_participant verified on hardware in 3a.2 manager-as-player toggle. |
| 012_user_preferences.sql | ✅ | (Session 5 Part 2c.1) | Mike | Proximity banner persistence (2c.2) ships against this; 2c shipped at commits `daa8718`, `0a3a9ea`, `e4a348e`, `5617689`. |
| 013_karaoke_session_helpers.sql | ✅ | (Session 5 Part 2d.0) | Mike | Explicitly recorded as shipped in `docs/SESSION-5-PART-2-CLOSING-LOG.md` row 2d.0. |
| 014_push_subscriptions.sql | ✅ | (Session 5 Part 2e.0) | Mike | Push notification infrastructure verified end-to-end on iPhone per `docs/CONTEXT.md` history. |
| 015_promotion_push_trigger.sql | ✅ | (Session 5 Part 2e.2; verified 2026-05-03) | Mike | Verified applied to prod 2026-05-03 via `SELECT tgname FROM pg_trigger WHERE tgname = 'trg_fire_promotion_push'` (returned 1 row) and `SELECT proname FROM pg_proc WHERE proname = 'fire_promotion_push'` (returned 1 row). Both objects defined in db/015 are present. Original application history was unclear at the time of MIGRATIONS_APPLIED.md's creation; this verification closes that uncertainty. |
| 016_remove_participant.sql | ✅ | 2026-05-02 | Mike | Applied manually via Supabase SQL Editor during 3a.2 hardware verification. Committed to repo at `05d2cae` earlier in Session 5 Part 3a planning; prod application slipped — caught when item 5 of the 3a.2 verification gate failed with 404 on the missing `rpc_session_remove_participant`. This file's existence was triggered by that gap. |
| 017_set_my_participation_role.sql | ✅ | 2026-05-02 | Mike | Applied manually via Supabase SQL Editor 2026-05-02 immediately after migration commit `8c83b35` landed. Verified via `SELECT proname, pg_get_function_arguments(oid) FROM pg_proc WHERE proname = 'rpc_session_set_my_participation_role'` returning the function with args `(p_session_id uuid, p_role text)`. |
| 018_session_start_active_default.sql | ✅ | 2026-05-02 | Mike | Applied manually via Supabase SQL Editor 2026-05-02 alongside the v2.105 doJoin restructure (same commit). Verified via `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_start';` returning the function with the new `case when p_app = 'games' then 'active' else 'audience' end` branched insert; comment also confirmed updated. Per `docs/GAMES-CONTROL-MODEL.md` § 2.4.4 for games branch; karaoke schema-state semantics preserved per `docs/KARAOKE-CONTROL-MODEL.md` § 1. |
| 019_trivia_premium_usage.sql | ✅ | 2026-05-04 | Mike | Applied manually via Supabase SQL Editor 2026-05-04 immediately after Phase 2 Commit A (`c4af15c`) landed. Verified via three queries: (1) `SELECT EXISTS (...) WHERE table_name = 'trivia_premium_usage'` returned `t`; (2) `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename = 'trivia_premium_usage'` returned `t` (RLS enabled); (3) `SELECT COUNT(*) FROM pg_policies WHERE tablename = 'trivia_premium_usage'` returned `0` (no policies — service-role bypass is the only access path, browser denied entirely). Trivia Phase 2 generate-trivia Edge Function unblocked for deploy. |
| 020_admission_model_v2.sql | ✅ | 2026-05-15 | Mike | Applied manually via Supabase SQL Editor 2026-05-15 after migration commit `80a141b` landed. Implements W1 from `docs/ADMISSION-MODEL-V2.md` § 10. All 7 post-migration verification queries (per the file footer) passed: (1) `SELECT DISTINCT admission_mode FROM sessions` returned only NULL — old values transitioned out, W2 will re-stamp `'open'`/`'gated'` at game-start; (2) `SELECT DISTINCT participation_role FROM session_participants` returned `active`/`audience`/`queued` (enum preserved); (3) `SELECT COUNT(*) FROM session_participants WHERE participation_role = 'queued' AND wanting_since IS NULL` returned `0` (backfill applied); (4) `\d session_participants` confirmed `wanting_since timestamp with time zone` column added; (5) `\d sessions` confirmed `admission_mode` is nullable; (6) `\d sessions` confirmed new `sessions_admission_mode_check` CHECK constraint allows `NULL OR ('open','gated')`; (7) `\d session_participants` confirmed `queue_position` column + `session_participants_queue_idx` partial unique index both still present (karaoke dependencies untouched per § 2.7). W2-W10 client code changes pending. |
| 021_session_set_admission_mode.sql | ✅ | 2026-05-15 | Mike | Applied manually via Supabase SQL Editor 2026-05-15 after migration commit `103b507` landed. Implements W2 from `docs/ADMISSION-MODEL-V2.md` § 10. New manager-only SECURITY DEFINER RPC `rpc_session_set_admission_mode(p_session_id, p_admission_mode, p_capacity)` for stamping admission_mode + capacity onto the sessions row at game-start (and clearing on Switch Game). Verified via three queries: (1) `SELECT proname FROM pg_proc WHERE proname = 'rpc_session_set_admission_mode'` returned 1 row (function exists); (2) `SELECT proargnames FROM pg_proc WHERE proname = 'rpc_session_set_admission_mode'` returned `{p_session_id, p_admission_mode, p_capacity}` (signature matches spec); (3) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_set_admission_mode'` returned the full function body including `SECURITY DEFINER`, `SET search_path = public`, all three error paths (42501 non-manager, 02000 missing/ended session, 22023 invalid admission_mode value), and the UPDATE that bumps `last_activity_at`. Validates against `sessions_admission_mode_check` value set (NULL/'open'/'gated'). Called from `games/player.html`'s `managerStartTrivia` / `managerStartLastCard` / `managerStartEuchre` / `managerSwitchGame` (W2 client-side wiring shipped in commit `103b507` alongside the migration file). |
| 022_session_update_participant_wanting_since.sql | ✅ | 2026-05-15 | Mike | Applied manually via Supabase SQL Editor 2026-05-15 after migration commit `b664106` landed. Part of W4 from `docs/ADMISSION-MODEL-V2.md` § 10. `CREATE OR REPLACE FUNCTION` on `rpc_session_update_participant` to populate the `wanting_since` column. Verified via `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_update_participant'`, confirming three checkpoints in the function body: (1) `v_new_wanting_since timestamptz` declaration present in the DECLARE block alongside `v_new_queue_position`; (2) the compute block branches all four transition cases correctly (`NULL p_participation_role` → preserve existing; non-queued → queued → `now()`; queued → queued → preserve existing; queued → non-queued → `NULL`); (3) the UPDATE statement writes both `queue_position = v_new_queue_position` and `wanting_since = v_new_wanting_since` in the same statement. All other behavior (manager/host/self transition rules, capacity check, queue_position auto-assignment, pre_selections, control_role gates) preserved identically per the CREATE OR REPLACE pattern. |
| 023_session_get_participants_wanting_since.sql | ✅ | 2026-05-15 | Mike | Applied manually via Supabase SQL Editor 2026-05-15 after migration commit `b664106` landed (paired with db/022 in the same prod apply session). Part of W4 from `docs/ADMISSION-MODEL-V2.md` § 10. `DROP FUNCTION IF EXISTS` + `CREATE FUNCTION` on `rpc_session_get_participants` to add `wanting_since` to the `RETURNS TABLE` and SELECT. Verified via four checkpoints: (1) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_get_participants'` confirmed `wanting_since timestamptz` present in the `RETURNS TABLE` column list between `queue_position` and `pre_selections`; (2) same query confirmed `sp.wanting_since` present in the SELECT list of the function body; (3) `SECURITY DEFINER` + `SET search_path = public` preserved across the DROP+CREATE (visible in same `pg_get_functiondef` output); (4) `SELECT has_function_privilege('authenticated', 'rpc_session_get_participants(uuid)', 'EXECUTE')` returned `t` confirming the GRANT was successfully re-applied inside the migration transaction after the DROP. Server-side `ORDER BY` unchanged — karaoke continues to use `queue_position` ordering; games-side client code will sort the queued bucket by `wanting_since` in `renderRoster` (W4 3C work). |
| 024_session_heartbeat.sql | ✅ | 2026-05-19 | Mike | Applied manually via Supabase SQL Editor 2026-05-19 after migration commit `02cdc08` landed. Implements W9 from `docs/ADMISSION-MODEL-V2.md` § 10 — implicit-leave detection. All four static schema checks passed: (1) `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_name='session_participants' AND column_name='last_seen_at'` returned 1 row with `timestamp with time zone`, `NO`, `now()`; (2) `SELECT indexname, indexdef FROM pg_indexes WHERE indexname='session_participants_heartbeat_idx'` returned 1 row with indexdef containing `WHERE (left_at IS NULL)`; (3) `SELECT proname, prorettype::regtype, proargnames FROM pg_proc WHERE proname='rpc_session_heartbeat'` returned `integer` rettype and `{p_session_id}` argnames; (4) `SELECT has_function_privilege('authenticated', 'rpc_session_heartbeat(uuid)', 'EXECUTE')` returned `t`. Prune mechanism verified end-to-end via manual back-date test: a peer participant's `last_seen_at` was set to `now() - 120s`, the prune UPDATE (substituting a known user_id for `v_user_id` since SQL Editor superuser context lacks `auth.uid()`) correctly set the peer's `left_at = now()` and the `user_id <> v_user_id` self-skip guard preserved Mike's row. Test data restored to pre-test state. Auth gate validated as a side effect: direct RPC call from superuser SQL Editor context (no `auth.uid()`) raised exception with errcode `42501` per the function's first authentication check. RPC end-to-end smoke test (calling `rpc_session_heartbeat` from a real authenticated session) deferred to W9 client wiring commit, where authenticated app users will exercise the full path during normal 20-second heartbeat operation. |
| 025_rooms_and_session_anchor.sql | ✅ | 2026-05-21 | Mike | Applied manually via Supabase SQL Editor 2026-05-21 after migration commit `dddbeb6` landed. Phase-1 schema migration introducing the durable `rooms` entity, demoting `sessions` to a per-app instance under a room, and re-anchoring `session_participants` from session_id to room_id. Built against `docs/PHASE-1-BUILD-SPEC.md` as amended by `c54d09e`. Schema-only — no RPC bodies written or rewritten; the only RPC/trigger statements are two DROPs (dead `rpc_session_promote_self_from_queue` per build spec §A, and `trg_fire_promotion_push` + `fire_promotion_push()` per §A's MISSED row, with recreation deferred to db/026 onward pending an iOS-app audit on payload shape). Resolved open questions folded in: OQ1 (partial-UNIQUE `rooms.room_code` index), OQ2 (no `rpc_session_start` change in this migration), OQ3 (`sessions.current_state` kept), OQ4 (`is_session_*` compat wrappers included and kept through Phase 1). All 11 verification queries from section 14 of the migration file passed: (1) `SELECT count(*) FROM public.rooms` returned 0; (2) `\d public.sessions` confirmed `room_id` present (NOT NULL, FK -> rooms) and `tv_device_id` / `manager_user_id` / `room_code` absent, with `current_state` still present per OQ3; (3) `\d public.session_participants` confirmed `room_id` present (NOT NULL, FK -> rooms) and `session_id` absent, all other columns (`id`, `user_id`, `control_role`, `participation_role`, `pre_selections`, `queue_position`, `joined_at`, `left_at`, `wanting_since`, `last_seen_at`) unchanged from db/024 baseline; (4) `SELECT indexname FROM pg_indexes WHERE tablename = 'rooms'` returned the 3 expected entries (`rooms_one_active_per_code`, `rooms_one_active_per_screen`, `rooms_pkey`); (5) `SELECT proname FROM pg_proc WHERE proname LIKE 'is_room%'` returned the 3 expected helpers (`is_room_participant`, `is_room_tv_household_admin`, `is_room_tv_household_member`); (6) `SELECT 1 FROM pg_proc WHERE proname = 'rpc_session_promote_self_from_queue'` returned 0 rows (dropped); (7) `SELECT 1 FROM pg_proc WHERE proname = 'fire_promotion_push'` returned 0 rows (dropped; recreation tracked in build spec §D for db/026 onward); (8) `SELECT 1 FROM pg_trigger WHERE tgname = 'trg_fire_promotion_push'` returned 0 rows (dropped); (9) `SELECT proname FROM pg_proc WHERE proname LIKE 'is_session_%'` returned the 3 expected compat wrappers (`is_session_participant`, `is_session_tv_household_admin`, `is_session_tv_household_member`) per OQ4; (10) `SELECT tablename, polname FROM pg_policies WHERE tablename IN ('rooms','sessions','session_participants')` returned the 3 expected SELECT policies, all room_id-keyed (no remaining `session_id` / `tv_device_id` / `manager_user_id` / `room_code` references in their quals); (11) `SELECT indexname FROM pg_indexes WHERE tablename = 'session_participants'` returned the 6 expected entries (`session_participants_pkey`, `_heartbeat_idx`, `_one_active_per_user`, `_one_manager`, `_queue_idx` all re-keyed to room_id, plus `_user_idx` unchanged from db/008 per build spec §E3). Phase-1 schema cutover complete. Temporary regression noted: queued→active push notifications do NOT fire until `fire_promotion_push` is recreated with room_id awareness (tracked in build spec §D, payload-shape decision pending iOS-app audit). Next: db/026 onward for the 14 room-keyed RPCs and the new `rpc_room_create`. |
| 026_room_keyed_rpcs_part1.sql | ✅ | 2026-05-22 | Mike | Applied manually via Supabase SQL Editor 2026-05-22 after migration commit `9e3926e` landed. Initial apply attempt raised SQLSTATE `42P13` ("cannot change name of input parameter \"p_session_id\"") on the first statement (rpc_session_join) and the BEGIN/COMMIT envelope rolled back the migration in full; root cause was that the file used `CREATE OR REPLACE FUNCTION` for renaming `p_session_id` → `p_room_id` on 7 of the 8 mechanical RPCs (Postgres requires DROP FUNCTION first to rename input parameters). Forward-correction commit `c657c9f` added `DROP FUNCTION IF EXISTS public.<name>(<old arg types>);` immediately before CREATE for each renamed function (matching the existing pattern used by rpc_session_get_participants in db/023 / db/026). After `c657c9f` landed in repo, the migration applied cleanly. Phase-1 RPC migration batch 1 of 3 (db/026/027/028) per `docs/PHASE-1-BUILD-SPEC.md` §D. All 6 post-migration verification queries from the file footer passed: (1) `SELECT proname, proargnames FROM pg_proc WHERE proname IN (10 RPC names) ORDER BY proname` returned 10 rows with the 8 mechanical RPCs re-keyed to `p_room_id` and `rpc_session_end` + `rpc_session_set_admission_mode` keeping `p_session_id` (signature unchanged); (2) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_end'` confirmed `UPDATE public.sessions` + `UPDATE public.rooms` only, no `UPDATE public.session_participants` (participant sweep removed per build spec §D); (3) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_set_admission_mode'` confirmed body routes auth through `rooms.controller_user_id` with no `session_participants` control_role check; (4) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_karaoke_song_ended'` confirmed `FROM public.sessions WHERE room_id = p_room_id AND ended_at IS NULL ... FOR UPDATE` lock pattern per build spec §D's pinned decision; (5) `SELECT proname, pg_get_functiondef(oid) LIKE '%update public.rooms%' AS touches_rooms FROM pg_proc WHERE proname IN ('rpc_session_get_participants', 'rpc_session_heartbeat')` returned `touches_rooms = false` for both — neither RPC bumps `rooms.last_activity_at` (rpc_session_heartbeat deliberately so: a 20s heartbeat bumping the room clock would defeat the 10-min reclaim inactivity gate. rpc_session_heartbeat still writes `session_participants.last_seen_at` and prunes stale peers — those side effects are not bumps to the room timestamp and are out of scope for this check); (6) `SELECT has_function_privilege('authenticated', p.oid, 'EXECUTE') AS authed FROM pg_proc p WHERE p.proname IN (10 RPC names)` returned `authed = true` for all 10 rows — confirms the DROP+CREATE fix successfully re-issued grants for the 7 renamed mechanical RPCs (DROP FUNCTION removes its EXECUTE grant; each function's subsequent `GRANT EXECUTE TO authenticated` statement in the migration restored it). Next: db/027 (rpc_session_start split + rpc_room_create) and db/028 (rpc_session_leave + reclaim RPCs). |
| 027_room_keyed_rpcs_part2.sql | ✅ | 2026-05-22 | Mike | Applied manually via Supabase SQL Editor 2026-05-22 after migration commit `2465ff5` landed. Migration applied cleanly after forward-correction commit `c657c9f` added the missing `DROP FUNCTION IF EXISTS public.rpc_session_start(uuid, text, text, int, boolean, text, text);` (7-arg old signature) before the new 6-arg CREATE; without that DROP, Postgres would have rejected the parameter rename (`p_tv_device_id` → `p_room_id`) with SQLSTATE `42P13` (same root cause as db/026's initial failure). Phase-1 RPC migration batch 2 of 3 (db/026/027/028) per `docs/PHASE-1-BUILD-SPEC.md` §D — implements OQ2 (a): rpc_session_start split into room-creation (new `rpc_room_create`) + session-creation (rewritten `rpc_session_start`). All 6 post-migration verification queries from the file footer passed: (1) `SELECT proname, proargnames FROM pg_proc WHERE proname IN ('rpc_room_create', 'rpc_session_start') ORDER BY proname` returned 2 rows with `rpc_room_create` proargnames `{p_screen_ref}` and `rpc_session_start` proargnames `{p_room_id, p_app, p_admission_mode, p_capacity, p_ask_proximity, p_turn_completion}` (6 args, down from db/018's 7 — confirms `p_tv_device_id` and `p_room_code` dropped, `p_room_id` added); exactly one `rpc_session_start` row, no leftover 7-arg overload — confirms `c657c9f`'s DROP FUNCTION successfully removed the old live signature before the new CREATE; (2) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_room_create'` confirmed body INSERTs into `public.rooms` with both `controller_user_id` and `owner_user_id` set to `v_user_id` (caller is convener) and inserts the convener row in `session_participants` with `control_role = 'manager'` and `participation_role = 'audience'`; (3) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_start'` confirmed body UPDATEs the controller's `session_participants` row with branched `participation_role` (`case when p_app = 'games' then 'active' else 'audience' end`) and unconditionally clears `queue_position` and `wanting_since`, with no `INSERT INTO public.session_participants` anywhere in the function — confirms the convener-seating split per `docs/ROOM-SESSION-MODEL.md`'s cross-app-move "nothing to copy" rule; (4) `SELECT column_default FROM information_schema.columns WHERE table_name = 'rooms' AND column_name = 'last_activity_at'` returned `now()` (rpc_room_create bumps via the INSERT default); `pg_get_functiondef(oid)` for `rpc_session_start` confirmed `update public.rooms set last_activity_at = now()` present (rpc_session_start bumps explicitly) — both RPCs satisfy the §D pinned activity-timestamp rule; (5) sole-writer guarantee on `rooms.owner_user_id`: first query attempt (`SELECT proname FROM pg_proc WHERE pg_get_functiondef(oid) ILIKE '%owner_user_id%' AND proname <> 'rpc_room_create' AND prokind = 'f' ORDER BY proname`) failed with SQLSTATE `42809` (`pg_get_functiondef()` cannot run on aggregate functions) because it scanned every schema and hit built-in aggregates; corrected query with the public-schema filter — `SELECT proname FROM pg_proc WHERE prokind = 'f' AND pronamespace = 'public'::regnamespace AND pg_get_functiondef(oid) ILIKE '%owner_user_id%' AND proname <> 'rpc_room_create' ORDER BY proname` — returned 0 rows, confirming `rpc_room_create` is the only function in the public schema that references `owner_user_id` (structural guarantee from PHASE-1-BUILD-SPEC.md OQ2 (a) and db/027's file header); (6) `SELECT has_function_privilege('authenticated', p.oid, 'EXECUTE') AS authed FROM pg_proc p WHERE p.proname IN ('rpc_room_create', 'rpc_session_start')` returned `authed = true` for both rows — confirms `rpc_session_start`'s DROP+CREATE re-issued its grant via the file's subsequent `GRANT EXECUTE TO authenticated` statement (matches the pattern verified for db/026's 7 renamed RPCs). Next: db/028 (rpc_session_leave + reclaim RPCs). |
| 028_room_keyed_rpcs_part3.sql | ✅ | 2026-05-22 | Mike | Applied manually via Supabase SQL Editor 2026-05-22 after migration commit `95dcf70` landed. Migration applied cleanly after forward-correction commit `c657c9f` added the three missing `DROP FUNCTION IF EXISTS public.<name>(uuid);` statements for rpc_session_leave, rpc_session_reclaim_manager, and rpc_session_admin_reclaim before each new CREATE; without those DROPs, Postgres would have rejected the parameter rename (`p_session_id` → `p_room_id`) — and for the two reclaim RPCs, also the return-type change from `sessions` to `rooms` — with SQLSTATE `42P13` (same root cause as db/026 and db/027's initial failures). Phase-1 RPC migration batch 3 of 3 (db/026/027/028) per `docs/PHASE-1-BUILD-SPEC.md` §D — **this commit completes the Phase-1 RPC migration arc; all three batches now applied to prod.** Implements ROOM-AUTHORITY-MODEL.md's four-tier host-first succession hierarchy in rpc_session_leave (with new optional `p_successor_user_id` parameter and the tier-4 full-room-end branch, the first and only writer of `rooms.ended_at` in the system). All 9 post-migration verification queries from the file footer passed: (1) `SELECT proname, proargnames, pg_get_function_result(oid) AS returns FROM pg_proc WHERE proname IN ('rpc_session_leave', 'rpc_session_reclaim_manager', 'rpc_session_admin_reclaim') ORDER BY proname` returned 3 rows with proargnames `{p_room_id, p_successor_user_id}` for rpc_session_leave (returns `session_participants`), `{p_room_id}` for rpc_session_reclaim_manager and rpc_session_admin_reclaim (both returns `rooms`); one row per RPC, no leftover sessions-returning overload — confirms `c657c9f`'s three DROPs successfully removed the old `(p_session_id uuid) RETURNS sessions` signatures before the new CREATEs; (2) sole-writer guarantee on `rooms.ended_at`: the raw footer query (`SELECT proname FROM pg_proc WHERE pg_get_functiondef(oid) ~* ... ORDER BY proname`) failed with SQLSTATE `42809` (`pg_get_functiondef()` cannot run on aggregate functions) because it scanned every schema; corrected query with the public-schema filter — `SELECT proname FROM pg_proc WHERE prokind = 'f' AND pronamespace = 'public'::regnamespace AND (pg_get_functiondef(oid) ~* 'rooms[[:space:]]+set[[:space:]]+ended_at' OR pg_get_functiondef(oid) ~* 'rooms\.ended_at[[:space:]]*=') ORDER BY proname` — returned 1 row (rpc_session_leave), confirming rpc_session_leave is the FIRST and ONLY writer of `rooms.ended_at` in the system (structural guarantee per db/028's file header, the counterpart to db/027's owner_user_id sole-writer guarantee); (3) sole-writer guarantee on `rooms.owner_user_id` preserved: the raw footer query without the schema filter again failed with `42809`; corrected query — `SELECT proname FROM pg_proc WHERE prokind = 'f' AND pronamespace = 'public'::regnamespace AND pg_get_functiondef(oid) ILIKE '%owner_user_id%' AND proname NOT IN ('rpc_room_create', 'rpc_session_leave', 'rpc_session_reclaim_manager', 'rpc_session_admin_reclaim') ORDER BY proname` — returned 0 rows, confirming rpc_room_create remains the only function in the public schema that substantively references `owner_user_id` (db/028's three RPCs mention it in comments only — those comments document the structural guarantee "owner_user_id is NOT touched" — but do not write it); (4) `SELECT proname, pg_get_functiondef(oid) ILIKE '%control_role = ''none''%' AS demotes, pg_get_functiondef(oid) ILIKE '%control_role = ''manager''%' AS promotes FROM pg_proc WHERE proname IN ('rpc_session_reclaim_manager', 'rpc_session_admin_reclaim')` returned `demotes = true` and `promotes = true` for both rows — confirms the demote-then-promote control_role mirror behavior preserved verbatim from db/010 (drives off the session_participants_one_manager partial-unique index, re-keyed to room_id by db/025); (5) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_reclaim_manager'` confirmed the 10-min inactivity gate reads `v_room.last_activity_at >= now() - interval '10 minutes'` and raises with errcode `'55000'` (object_not_in_prerequisite_state) — the gate reads `rooms.last_activity_at` (the §D pinned activity-target) not the dropped `sessions.last_activity_at`; (6) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_session_leave'` confirmed the four-tier succession SQL: tier 1 SELECT with `control_role = 'host'` and `LIMIT 1`, tier 2 SELECT with `user_id = p_successor_user_id` and `participation_role IN ('active', 'queued')` and `LIMIT 1`, tier 3 SELECT with `participation_role IN ('active', 'queued')` and `ORDER BY joined_at asc LIMIT 1`, tier 4 UPDATE `public.rooms SET controller_user_id = null, ended_at = now()` — confirms ROOM-AUTHORITY-MODEL.md's host-first hierarchy is in the body; (7) tier-4 write order verified by manual inspection of the same `pg_get_functiondef` output: three UPDATEs in the correct order — `UPDATE public.sessions ... ended_at` first, then `UPDATE public.session_participants ... left_at`, then `UPDATE public.rooms ... controller_user_id = null, ended_at = now()` last — confirms contents are cleaned up before the room is stamped ended, so no reader observes an ended room with live contents; (8) non-controller leave path verified by inspecting the `controller_user_id <> v_user_id` branch: NO `update public.rooms` statement present in that branch (only `update public.session_participants set left_at = now()` and `update public.sessions set last_activity_at = now()`) — confirms a non-controller participant departing does NOT bump `rooms.last_activity_at`, which would otherwise reset the reclaim 10-min inactivity gate on behalf of someone who is not the controller; (9) `SELECT p.proname, has_function_privilege('authenticated', p.oid, 'EXECUTE') as authed FROM pg_proc p WHERE p.proname IN ('rpc_session_leave', 'rpc_session_reclaim_manager', 'rpc_session_admin_reclaim') ORDER BY p.proname` returned `authed = true` for all 3 rows — confirms `c657c9f`'s DROP+CREATE pattern re-issued grants for all three RPCs via each function's subsequent `GRANT EXECUTE TO authenticated` statement in the migration (matches the pattern verified for db/026's 7 renamed RPCs and db/027's rpc_session_start). Phase-1 RPC migration arc complete: db/026 + db/027 + db/028 all applied to prod. Next: §F shell session-state cluster rework (PHASE-1-BUILD-SPEC.md §F, ~13 sites) — the resolving work for the shell-side runtime breakage from the RPC signature changes, tracked in `docs/DEFERRED.md`'s "Phase-1 RPC migration in-flight" entry. Per-surface karaoke and games call-site reworks ride Phase 3 and Phase 4 respectively per UNIFIED-APP-PLAN §5. |
| 029_fire_promotion_push_recreation.sql | ✅ | 2026-05-23 | Mike | Applied manually via Supabase SQL Editor 2026-05-23 after migration commit `b0f8a47` landed. Recreates the `fire_promotion_push()` trigger function and `trg_fire_promotion_push` trigger that were dropped by db/025 step 4 (the original db/015 implementation referenced `NEW.session_id`, the column db/025 removed from session_participants during the room-anchor re-keying). The recreated function reads `NEW.room_id` instead; the trigger's WHEN clause (`OLD.participation_role = 'queued' AND NEW.participation_role = 'active'` on `session_participants` AFTER UPDATE) is unchanged from db/015. Payload change: `jsonb_build_object('user_id', NEW.user_id, 'type', 'promotion', 'room_id', NEW.room_id)` — `session_id` is dropped entirely per the 2026-05-23 iOS audit (recorded in DEFERRED.md), which confirmed the Capacitor app's two push-notification listeners read nothing from `notif.data`; `room_id` is chosen for future-proofing per ROOM-SESSION-MODEL.md. Vault read uses `decrypted_secret` from `vault.decrypted_secrets` for both `edge_fn_url` and `service_role_key` — the latter name is misleadingly kept from db/015 (the entry now holds a custom PROMOTION_TRIGGER_SECRET, not the auto-provisioned service role key); the migration's header and function-body comments warn future readers off "fixing" the name. The send-push-notification Edge Function (`supabase/functions/send-push-notification/index.ts` line 244) was deployed in lockstep via `supabase functions deploy send-push-notification --no-verify-jwt` (the `--no-verify-jwt` flag is mandatory per CLAUDE.md doctrine — the trigger sends a non-JWT shared secret) so its synthesized APNs `data` object reads `room_id: body.room_id` to match the new trigger payload. All 5 static verification queries from the file footer passed: (1) `SELECT proname, pg_get_function_result(oid), prosecdef, pg_get_function_arguments(oid) FROM pg_proc WHERE proname = 'fire_promotion_push' AND pronamespace = 'public'::regnamespace` returned 1 row with returns = `trigger`, security_definer = `true`, args = empty; (2) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'fire_promotion_push'` confirmed the function body references `NEW.room_id` and includes `'room_id', NEW.room_id` in the `jsonb_build_object` call, with NO `NEW.session_id` or `session_id` token in the body; (3) `SELECT tgname, tgenabled, tgtype, pg_get_triggerdef(oid) FROM pg_trigger WHERE tgname = 'trg_fire_promotion_push'` returned 1 row with `tgenabled = 'O'` (origin-enabled, the normal case) and the trigger def containing `AFTER UPDATE OF participation_role` and the `OLD.participation_role = 'queued' AND NEW.participation_role = 'active'` WHEN clause; (4) `SELECT t.tgname, c.relname FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid WHERE t.tgname = 'trg_fire_promotion_push'` confirmed `table_name = 'session_participants'`; (5) `SELECT name FROM vault.decrypted_secrets WHERE name IN ('edge_fn_url', 'service_role_key')` returned 2 rows — both Vault names present (values not printed). End-to-end smoke test deferred until a real queued→active transition is exercised on a device with a registered push_subscriptions row — push DELIVERY infrastructure restored, ready to fire on the next queued→active UPDATE. **Scope honesty:** this migration + the Edge Function deploy restored push delivery only (user receives the "You're up! / Tap to take the stage" notification). It did NOT restore tap-to-route behavior — tapping the notification still just opens the app to wherever it was, because the `// TODO 2e.1+` routing implementation in `karaoke/singer.html`'s push listeners was never built. That work remains tracked as the separate DEFERRED entry "push-notification tap routing — `// TODO 2e.1+` in karaoke/singer.html never implemented" (still active). |
| 030_tv_devices_can_embed.sql | ✅ | 2026-05-23 | Mike | Applied manually via Supabase SQL Editor 2026-05-23 after migration commit `2ca15a8` landed. Adds a `can_embed boolean NOT NULL DEFAULT false` column to public.tv_devices — the activation predicate for the premium-control layer per ROOM-AUTHORITY-MODEL.md § "When the premium-control layer is active". Schema-only migration: no Edge Function deploy, no RPC body changes; the tv2.html self-report and claim-flow recording that would populate this column are deferred per DEFERRED.md (blocked on the compositing pipeline not yet existing as code). All 3 static verification queries from the file footer passed: (1) `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'tv_devices' AND column_name = 'can_embed'` returned 1 row with `data_type = boolean`, `is_nullable = NO`, `column_default = false` (the verbose Postgres render `'false'::boolean` would have been equally valid); (2) `SELECT can_embed, count(*) FROM public.tv_devices GROUP BY can_embed ORDER BY can_embed` confirmed every existing row (2 rows total in this project) received the conservative default `false`, with no `can_embed = true` row appearing — confirms the schema migration was the first writer of this column and no partial-application leakage exists; (3) `SELECT col_description('public.tv_devices'::regclass, ...)` returned the column comment recording the ROOM-AUTHORITY-MODEL.md citation, the conservative-default rationale, and the "no writer yet" status. Functional consequence: every row reads `can_embed = false`, so the premium-control layer is NEVER active in prod even when the C5 ownership-seize RPC eventually ships — that's the right interim safety posture (better to mis-classify a camera-equipped device as non-embedding than mis-classify a plain browser as embedding and have the embed fail at runtime). C5 is now unblocked schema-side: its auth gate (b) can read `tv_devices.can_embed = true` directly; ship-time of C5 is independent of the deferred self-report writer. Original C1 DEFERRED entry was re-scoped in the same commit (2ca15a8): the schema half is now done, the entry stays ACTIVE tracking only the self-report + claim-flow recording remainder. |
| 031_room_seize.sql | ✅ | 2026-05-23 | Mike | Applied manually via Supabase SQL Editor 2026-05-23 after migration commit `b654f91` landed. Creates the `rpc_room_seize(p_room_id uuid)` RPC — the ownership-seize operation specified in ROOM-AUTHORITY-MODEL.md § "Seize authority". Implements the C5 work item from the premium-control model documentation arc. Distinct from the existing inactivity-reclaim RPCs (`rpc_session_reclaim_manager`, `rpc_session_admin_reclaim` from db/028) — no inactivity gate; gated on ownership-class authority (convener-seize OR admin-seize) AND the premium-control-layer activation predicate (`rooms.screen_ref → tv_devices.can_embed = true`). Demote-then-promote mechanic copied verbatim from db/028 §§1 + 3. RPC-only migration: no Edge Function deploy needed. 4 of 5 static verification queries from the file footer passed; the 5th (functional smoke test) is DEFERRED until a real `can_embed = true` device exists in prod (the deferred self-report writer's job). Static verifications: (1) `SELECT proname, proargnames, pg_get_function_arguments(oid), pg_get_function_result(oid), prosecdef FROM pg_proc WHERE proname = 'rpc_room_seize' AND pronamespace = 'public'::regnamespace` returned 1 row with `proargnames = {p_room_id}`, `args = 'p_room_id uuid'`, `returns = rooms`, `security_definer = true`; (2) `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'rpc_room_seize' AND pronamespace = 'public'::regnamespace` confirmed all 5 guards present in body (authed check; room not found 02000; room ended 02000; screen_ref null 42501; can_embed = true 42501; convener-OR-admin self-join 42501) plus the demote-then-promote mechanic (`control_role = 'none'` demote + `control_role = 'manager'` promote) and the rooms write (`controller_user_id = v_user_id, last_activity_at = now()`); body contains no `UPDATE public.rooms SET owner_user_id =` or `INSERT INTO public.rooms (..., owner_user_id, ...)` — preservation guarantee intact; (3) `SELECT has_function_privilege('authenticated', p.oid, 'EXECUTE') FROM pg_proc p WHERE p.proname = 'rpc_room_seize' AND p.pronamespace = 'public'::regnamespace` returned `authed = true`; (4) owner_user_id sole-writer sentinel — `SELECT proname FROM pg_proc WHERE prokind = 'f' AND pronamespace = 'public'::regnamespace AND pg_get_functiondef(oid) ILIKE '%owner_user_id%' AND proname NOT IN ('rpc_room_create', 'rpc_session_leave', 'rpc_session_reclaim_manager', 'rpc_session_admin_reclaim', 'rpc_room_seize') ORDER BY proname` returned 0 rows, confirming rpc_room_create remains the sole WRITER of owner_user_id (per the db/027 structural guarantee); rpc_room_seize was added to the exclusion list because guard 5 READS `v_room.owner_user_id` (convener-seize check) without writing it. **Scope honesty / runtime-effect note:** db/031 SHIPPED the RPC — it exists in prod, has the correct shape, is callable. But guard 4b (`can_embed = true`) will REFUSE every seize attempt in prod until a `tv_devices.can_embed = true` row exists, which is the deferred self-report writer's job (still-active "tv_devices.can_embed self-report path" entry in DEFERRED.md). Both must land before seize becomes functionally usable in prod; the RPC's correctness is independent of any row's current `can_embed` value. The end-to-end smoke test (footer verification 5, with 6 sub-steps including manually setting `can_embed = true` on a test device) is deferred to whenever the self-report writer ships (or to a one-off test where the operator manually UPDATEs `can_embed = true` and reverts afterward). |
| 032_venue_abstraction_schema.sql | ✅ | 2026-05-24 | Mike | Applied manually via Supabase SQL Editor 2026-05-24 against the file landed in this same commit. Implements the database schema layer for Phase 2 (venue extraction → venue abstraction) per `docs/PHASE-2-BUILD-SPEC.md` §4.5. Additive-only — no existing column dropped or altered, no data cleared, no RPC or surface touched; Phase 2 ships DORMANT and the new tables are created empty. EXTENDED two existing tables: `venue_defaults` gains `camera_fov numeric`, `motion jsonb`, `ambient jsonb`; `karaoke_venue_settings` gains `camera_fov_override numeric`, `motion_override jsonb`, `ambient_override jsonb`, `anchor_patch jsonb` (delta-set patch per spec §4.4 — not a value override). CREATED three new tables: (a) `venue_anchors` — typed anchor records with §3.1 base fields (id, venue_id FK to venue_defaults ON DELETE RESTRICT, type with CHECK vocabulary {callout, pin, spotlight, particle, audio, video, link-hotspot}, yaw_deg/pitch_deg nullable with paired-NULL CHECK for screen-space anchors, label, start_sec/end_sec, link jsonb, payload jsonb, is_broken validity flag, created_at/updated_at/updated_by) + 2 indexes (venue_id; venue_id+type) + RLS public-read/is_platform_admin-write + set_updated_at trigger; (b) `costumes` cross-app library — id, name NOT NULL, category/slot unconstrained text (vocabulary deferred until cross-app slot semantics settle), asset_ref NOT NULL, is_broken, lifecycle columns + RLS + set_updated_at trigger; (c) `venue_suggested_costumes` association — venue_id FK RESTRICT, costume_id FK RESTRICT, position integer default 0 with no uniqueness constraint on (venue_id, position) so ties are allowed, composite PK (venue_id, costume_id) + composite index (venue_id, position) + RLS. Two key design decisions recorded in the migration header: (1) `motion` is a single jsonb column (not separate `rotation_mode text` + `rotation_deg_per_sec numeric`) — same OQ2 rationale extended to motion: parameter sets vary by motion type (`static` needs nothing, `orbit` carries a numeric speed, future `elliptical` would carry radii + tilt); recognized types today are `static` and `orbit`, others extend without schema change; speed is always a number inside the jsonb (never a word); no CHECK constraint at the column level. (2) All three authored-content FKs (venue_anchors.venue_id, venue_suggested_costumes.venue_id, venue_suggested_costumes.costume_id) use ON DELETE RESTRICT (not CASCADE) — venues and costumes are stable enumerated content today, but anchors and suggested-costume associations are AUTHORED CONTENT; RESTRICT blocks a venue/costume delete at the database level when dependents exist, forcing deliberate cleanup, rather than silently destroying authored content (CASCADE would be exactly the wrong default for editable content). All 12 footer verification queries passed — both existing tables extended additively (existing columns untouched), 3 new tables created (venue_anchors, costumes, venue_suggested_costumes), all empty, RLS + policies + triggers + FK RESTRICT confirmed. Verification (11) confirmed venue_defaults row count = 26 (matches the db/003 seed; no drift between repo seed, current `venues.json`, and prod). **Phase-2 ships DORMANT — scope-honest note:** Phase 2's deliverable per spec §10 is the abstraction itself, not its consumers. No karaoke rewire (deferred to Phase 3), no games adoption (Phase 4), no admin authoring UI (settled as fast-follow per spec §7), no shell/venue-*.js modules yet (next task). The C2 surface-side completion and schema-stale sessions SELECTs on the four pre-Phase-3 surfaces (DEFERRED.md entries from the prior session) remain unaffected by this migration and continue to track Phase 3/4. Migration spec source: `docs/PHASE-2-BUILD-SPEC.md` (committed); pre-write consultation: 2026-05-24 V1-V8 findings against the prior repo state; planning-chat DDL review folded in two pre-apply revisions (motion jsonb + FK RESTRICT). |
| 036_particle_anchor_seed.sql | ✅ | 2026-05-26 | Mike | Applied manually via Supabase SQL Editor 2026-05-26 against the file landed in this same commit. Third migration in the Venue Admin UI workstream (Stage A3 per `docs/VENUE-ADMIN-UI-A3-BUILD-SPEC.md` §2). SEED-ONLY migration: no RPCs created. Per spec §2.1, db/035's `rpc_venue_anchor_upsert` and `rpc_venue_anchor_delete` are type-agnostic (db/035 declares `v_known_types` including `'particle'` and validates against the full vocabulary), so db/036 inserts 4 particle anchor rows via `INSERT ... ON CONFLICT (id) DO NOTHING` (the db/035 seed pattern) and adds no new functions. One transactional file. Deterministic ids `anc_par_<venue_id>` for the 4 in-scope venues per spec §0.3's locked enumeration: stadium (kind=point-cloud, position_layout=cartesian, 400 phone-light dots), disco (kind=point-cloud, position_layout=polar-projected, 120 mirror-ball dots with rotation + two-modulator binding [beat_scale→size, beat_brightness→alpha]), speakeasy (kind=volumetric, 35 smoke-wisps with size_growth + fade + turbulence + per-particle alpha_init_range), festival (kind=directional-emitter, 80 confetti rects falling from top with rotation). label='Particles' for all 4. honkytonk has NO particle anchor — its procedural particles were deleted per the comment at `karaoke/stage.html:4832`. Position-consistency: all 4 anchors have yaw_deg=NULL and pitch_deg=NULL (2D-canvas screen-space overlays, not sphere-pinned) — satisfies db/032's type-agnostic `venue_anchors_position_consistency` CHECK constraint (`(yaw_deg IS NULL) = (pitch_deg IS NULL)`) exactly as db/035's audio seed did. Payloads incorporate the §1 amendment (commit `716746b`): alpha pulled from color strings (color fragments are RGB only; alpha is the scalar `alpha` field or per-particle `alpha_init_range`), modulator allowed in array form (disco binds two), `twinkle_phase_speed_range` optional on point-cloud (disco omits — no internal twinkle, only external beat modulation), `polar.dist_range` normalized against `Math.max(canvas.width, canvas.height)` per the procedural source basis. All three verification queries from the file footer PASS against prod 2026-05-26 first run: Q1 (`SELECT count(*) FROM public.venue_anchors WHERE type = 'particle'`) returned `particle_count = 4`; Q2 (per-venue id/kind/label, ordered by venue_id) returned exactly the 4 expected rows with correct kind and `label='Particles'` for each; Q3 (payload schema spot-check) returned the correct `kind_specific_marker` per row (disco: polar-projected, festival: top, speakeasy: spawn_region present, stadium: cartesian). No anon/grant verification queries needed — db/036 creates no functions; db/035's RPCs already carry their grant surface (anon-revoke baked in from the start per the Stage A1 Bug 2 doctrine). **Companion surface changes (this same commit):** `shell/venue-renderers/particle.js` (new file — the particle anchor renderer per spec §3, self-contained with its own particle array + RAF loop + canvas reference passed via ctx, three-kind dispatch with point-cloud's position_layout sub-branch, all four deferred renderer-contract items implemented: CLR-5 point-cloud alpha formula `clamp(alpha * (0.2 + sin(phase)*0.8) * modulator_alpha, 0, 1)`, CLR-6 turbulence `vx += (random()-0.5) * strength`, CLR-7 polar normalization against `max(canvas.width, canvas.height)`, CLR-8 polar-projected canvas-clip; built-in preview oscillators for `crowd_brightness`/`beat_scale`/`beat_brightness` so the admin preview looks alive without the real modulator system being wired); `admin-venues.html` (particle anchor authoring panel per §4 — per-kind form with kind selector swapping between point-cloud / directional-emitter / volumetric sections, position_layout sub-swap between cartesian / polar-projected within point-cloud, bounded 2D-canvas preview surface with Play/Replay/Stop affordances, anchor-level dirty tracking via `state.particleAnchorDirty` Set (not field-level — payload reconstructed from form state at save/preview time, full-payload-replace via `p_partial.payload` per db/035's UPDATE clause), 8-second "Preview ended — click Replay" soft-timeout for respawn:false effects, PREVENT multi-anchor rule mirroring A2 — '+ Add particle anchor' button hidden when count ≥ 1, first v2.NN version stamp at `v2.138` matching the shell-adjacent numbering; beforeunload + venueHasAnyDirty + discardAllForVenue extended to include particle anchor dirty state); `karaoke/stage.html` (one new `<script type="module" src="../shell/venue-renderers/particle.js"></script>` tag at line 17 — registration only, not a reader-path change per D8 and the A2 Check 12 precedent; no version bump per the A2 precedent for registration-only changes). **Stage A3 is AMBIENT_PROFILES-first per D8:** the 4 seeded particle anchors are dormant data; karaoke/stage.html continues reading from AMBIENT_PROFILES + addVenueEffects3D until Stage 6 (the AMBIENT_PROFILES retirement) promotes anchors to load-bearing. **Re-staging per spec §0.2:** A3 covers 2D-canvas particles only; the 3D particle paths (stadium's 2000 `THREE.Points` phone-lights inside `buildStadiumEffects3D` + speakeasy's 60 sphere-mesh smoke clouds inside `buildSpeakeasyEffects3D`) defer to a later stage paired with A5's Three.js builder work, so the 3D rendering context is solved once by whoever is already in the Three.js builders. **Deferred items A3 generates** (to be filed at A3 closeout per spec §7, NOT in this commit): P1 / "drifting-cloud" 4th particle kind (the drift+wrap+twinkle pattern from the ghost-venue cross-check); venue modulator system (the real wiring of GSAP-driven scalars that A3 only records bindings for); particle panel validates JSON well-formedness only, not §1 schema shape (noted as acceptable per A3 — the renderer degrades gracefully on schema-shape errors rather than crashing). |
| 035_audio_anchor_rpcs_and_seed.sql | ✅ | 2026-05-26 | Mike | Applied manually via Supabase SQL Editor 2026-05-26 against the file landed in this same commit. Second migration in the Venue Admin UI workstream (Stage A2 per `docs/VENUE-ADMIN-UI-A1-BUILD-SPEC.md` §7). Three sections in one transactional file (mirrors db/003's structure): (1) `rpc_venue_anchor_upsert(p_id text, p_venue_id text, p_partial jsonb)` — INSERT-or-UPDATE on the id column, SECURITY DEFINER, is_platform_admin-gated, supports the admin UI's create + edit operations; (2) `rpc_venue_anchor_delete(p_id text)` — delete by id, same gating, supports the admin UI's delete + the §8.2 Check 9 round-trip; (3) audio anchor seed of 19 rows — one per audio-only venue per spec §2's locked inventory, `INSERT ... ON CONFLICT (id) DO NOTHING` for idempotency, deterministic ids `anc_aud_<venue_id>`. Five distinct error codes per the file header (42501, 02000, 22023, 22004, P0002). UPDATE-or-INSERT branch via existence-driven check on id; UPDATE path uses the jsonb `?` key-exists operator (same pattern as db/034) so absent keys preserve existing column values; INSERT path requires `type` in p_partial (NOT NULL column on the table). Type vocabulary validated defensively against `venue_anchors_type_check` from db/032:254-258 — the v_known_types array in the RPC body is verbatim the CHECK constraint's vocabulary (`callout`, `pin`, `spotlight`, `particle`, `audio`, `video`, `link-hotspot`); the RPC and the DB CHECK cannot drift. Audio anchor seed encodes the kids-dino2 shared-sound exception (sound_id='kids-dino' for venue_id='kids-dino2') matching the AMBIENT_PROFILES entry at karaoke/stage.html:4990; enchantedforest uses sound_id='enchantedforest' (NOT 'forest') per OQ-S1's resolution in spec §10. **Grant surface — REVOKE FROM PUBLIC + REVOKE FROM anon + GRANT TO authenticated per RPC, baked in from the start of this migration** (not a post-apply fix as in db/034's history). The Stage A1 verification log's Bug 2 doctrine — Supabase's ALTER DEFAULT PRIVILEGES auto-grants EXECUTE directly to anon, REVOKE FROM PUBLIC alone is a no-op in Supabase, REVOKE FROM anon is the load-bearing fix — is applied prophylactically here. service_role retains EXECUTE intentionally. All 8 footer verification queries passed against prod 2026-05-26 on first run, including the anon-grant checks (queries 3 and 6 returned `anon_authed=false` on first try, unlike db/034 where Q3 initially failed and surfaced the doctrine): (1) `SELECT proname, prosecdef, pg_get_function_result(oid), pg_get_function_arguments(oid) FROM pg_proc WHERE proname = 'rpc_venue_anchor_upsert' AND pronamespace = 'public'::regnamespace` returned 1 row with `prosecdef = true`, `returns = public.venue_anchors`, `args = 'p_id text, p_venue_id text, p_partial jsonb'`; (2) `has_function_privilege('authenticated', 'public.rpc_venue_anchor_upsert(text, text, jsonb)'::regprocedure, 'EXECUTE')` returned `authed = true`; (3) `has_function_privilege('anon', ...)` returned `anon_authed = false` (first try — anon-revoke baked in); (4) same shape for rpc_venue_anchor_delete with `returns = void` and `args = 'p_id text'`; (5)+(6) same authed/anon_authed results for the delete RPC; (7) `SELECT count(*) FROM public.venue_anchors WHERE type = 'audio'` returned `audio_count = 19`; (8) per-venue sound_id correctness — 19 rows ordered by venue_id with sound_id matching venue_id for 18 of them, sound_id='kids-dino' for venue_id='kids-dino2' (the shared-sound exception), sound_id='enchantedforest' for venue_id='enchantedforest' (per OQ-S1), label='Ambient' for all 19. **Companion surface changes:** `shell/venue-renderers/audio.js` (new file — the audio anchor renderer per spec §7.1's design-fork option (b), self-contained with its own AudioContext/GainNode/source state, registered to the anchor registry at module load); `admin-venues.html` (audio anchor authoring panel per §7.2 — load via loadVenueAnchors, per-row create/edit/delete lifecycle, in-surface Play preview, PREVENT decision on multi-anchor per Item 3 of the implementation pass — '+ Add audio anchor' button hidden when count ≥ 1, with explanatory note); `karaoke/stage.html` (one new `<script type="module" src="../shell/venue-renderers/audio.js"></script>` tag — registration only, not a reader-path change per D8 and explicitly permitted per spec §8.2 Check 12). **Stage 2 is AMBIENT_PROFILES-first per D8:** the 19 seeded anchors are dormant data; karaoke/stage.html continues reading from AMBIENT_PROFILES until Stage 6 (the AMBIENT_PROFILES retirement) promotes anchors to load-bearing. Companion spec correction (commit 647c31b, immediately preceding) fixed §8.2 Check 8's incorrect `window.getAnchorRenderer` reference — the registry is exposed on `window.elsewhere.anchorRegistry`. |
| 034_venue_default_update_rpc.sql | ✅ | 2026-05-26 | Mike | Applied manually via Supabase SQL Editor 2026-05-26 against the file landed in this same commit. Single new SECURITY DEFINER RPC `rpc_venue_default_update(p_venue_id text, p_partial jsonb)` returning `public.venue_defaults` — the write path for the Venue Admin UI Stage 1 surface (admin-venues.html) per `docs/VENUE-ADMIN-UI-A1-BUILD-SPEC.md` §6. UPDATE-only by construction: all 26 venue_defaults rows are pre-seeded by db/003:126-152; the admin UI edits those rows, never creates new ones, and creating new venues is post-Phase-5 Part 2 work. Eliminating the INSERT branch removes the silent-zero failure mode where an INSERT with partial yaw/pitch would default the NOT NULL columns to 0 via coalesce, mis-seeding a venue's camera. Five distinct error codes per the header's error-code enumeration: 42501 (insufficient_privilege — not authenticated or not is_platform_admin); 02000 (no_data — p_venue_id null/empty input); 22023 (invalid_parameter_value — unknown key in p_partial); 22004 (null_value_not_allowed — NULL passed for a NOT NULL column); P0002 (no_data_found — p_venue_id well-formed but references no existing row). Step 6's row-not-found raise was changed from 02000 to P0002 during the spec/apply cycle to make verification query 3's diagnostic unambiguous against step 3's input-absent raise. Body uses the jsonb `?` key-exists operator (not just `->>` casting) so absent keys preserve the existing column value while explicit NULL on nullable columns (camera_fov / motion / ambient) clears them. Schema unchanged: no new tables, no new columns, no new indexes, no new RLS policies; the existing "venue_defaults: admin write" RLS policy (db/003:69-73 + db/004:54-55 renamed to is_platform_admin) remains as defense-in-depth for direct table writes, while this RPC's own is_platform_admin check is the authoritative gate for the admin-UI write path. **Grant surface — two REVOKEs + one GRANT.** The migration revokes EXECUTE from both `public` and `anon` before granting to `authenticated`. The REVOKE FROM `public` is defensive against PostgreSQL's own CREATE FUNCTION default (auto-grants EXECUTE to PUBLIC); in Supabase this default has already been revoked at project setup so the statement is a no-op there but the migration stays correct against a fresh non-Supabase PostgreSQL replay. The REVOKE FROM `anon` is the load-bearing fix for the Supabase environment: Supabase's ALTER DEFAULT PRIVILEGES on the public schema auto-grants EXECUTE on every new function DIRECTLY to anon, authenticated, and service_role — these are direct role grants, NOT inherited from PUBLIC, so REVOKE FROM PUBLIC does not affect them. The REVOKE FROM PUBLIC was added to the migration first during the spec/apply cycle as a defensive measure; the diagnostic against prod showed `anon` retained EXECUTE via the Supabase default-privileges direct grant, so the REVOKE FROM anon was added as the actual fix. `service_role` retains EXECUTE intentionally (backend-key role for server-side Supabase operations). The function's own auth.uid() gate still raises 42501 for any anon caller at runtime so the grant-level revoke is defense-in-depth rather than a data-exposure mitigation. Both REVOKEs (the defensive PUBLIC one + the load-bearing anon one) and the explicit GRANT TO authenticated are now in the committed file. All 4 footer verification queries passed against prod 2026-05-26: (1) `SELECT proname, prosecdef, pg_get_function_result(oid), pg_get_function_arguments(oid) FROM pg_proc WHERE proname = 'rpc_venue_default_update' AND pronamespace = 'public'::regnamespace` returned 1 row with `prosecdef = true`, `returns = public.venue_defaults`, `args = 'p_venue_id text, p_partial jsonb'`; (2) `SELECT has_function_privilege('authenticated', 'public.rpc_venue_default_update(text, jsonb)'::regprocedure, 'EXECUTE') AS authed` returned `authed = true`; (3) `SELECT has_function_privilege('anon', 'public.rpc_venue_default_update(text, jsonb)'::regprocedure, 'EXECUTE') AS anon_authed` returned `anon_authed = false` (after the REVOKE FROM anon was applied — initially returned true under just the REVOKE FROM PUBLIC, which is what surfaced the Supabase default-privileges direct-grant mechanism); (4) `SELECT (pg_get_functiondef(oid) ILIKE '%insert%into%venue_defaults%') AS has_insert` returned `has_insert = false` confirming the function is UPDATE-only by construction. **Companion surface change:** the Venue Admin UI Stage 1 surface (`admin-venues.html` at repo root) landed in the same commit as this migration entry — calls `rpc_venue_default_update` to write venue_defaults edits with explicit-save dirty-state tracking. **Stage 2 is separate:** the audio renderer impl + audio anchor authoring + 19 audio-only venue translations + the two anchor RPCs (`rpc_venue_anchor_upsert`, `rpc_venue_anchor_delete` in db/035) are Stage 2 scope per `docs/VENUE-ADMIN-UI-A1-BUILD-SPEC.md` §7 and ship as their own propose-pause cycle. **Broader defense-in-depth flag for future cleanup:** the same Supabase ALTER DEFAULT PRIVILEGES mechanism affects every prior SECURITY DEFINER RPC in db/006 onward — they all rely on Supabase's default direct-grant to authenticated (additive — already granted) and never REVOKE FROM anon, so the entire prior RPC surface has grant-level EXECUTE for anon. No actual exposure (every RPC body raises 42501 for anon as the first executable check), but defense-in-depth gap worth filing as a DEFERRED entry for a sweep REVOKE FROM anon migration. |
| 037_spotlight_anchor_seed.sql | ✅ | 2026-05-27 | Mike | Applied manually via Supabase SQL Editor 2026-05-27 against the file landed in this same commit. Fourth migration in the Venue Admin UI workstream (Stage A4a per `docs/VENUE-ADMIN-UI-A4A-BUILD-SPEC.md` §2). SEED-ONLY migration: no RPCs created. Per spec §2.1, db/035's `rpc_venue_anchor_upsert` and `rpc_venue_anchor_delete` are type-agnostic (db/035 declares `v_known_types` including `'spotlight'` and validates against the full vocabulary), so db/037 inserts 3 spotlight anchor rows via `INSERT ... ON CONFLICT (id) DO NOTHING` (the db/035 seed pattern) and adds no new functions. One transactional file. Deterministic ids `anc_spot_<venue_id>` for the 3 in-scope venues per spec §0.3's locked enumeration: stadium (kind=swept-beam-2d, count:4 — 4 sweeping spotlight beams pivoting at top-center with per-beam hues [220,200,240,180] + GSAP-equivalent power1.inOut sweep tweens 1.5-4s with i*800ms stagger + 3-stop gradient at 0/0.5/1 with mid alpha×0.4), festival (kind=pulsed-laser, count:6 — 6 beat-pulsing lasers pivoting at bottom-center, BPM=128, per-laser hues + drift speeds [-0.005, 0.005, -0.005, 0.005, -0.005, 0.005] *negative-first per the source's (i%2 ? 1 : -1)*0.005 evaluating to -1 at i=0* + first_pulse_offset_beats=0.5 + attack/decay envelope at power3.out 60ms → power2.in 380ms + 3-stop gradient at 0/0.6/1 with mid alpha×0.3), speakeasy (kind=light-shaft, count:3 — 3 amber light shafts at x_init_norm [0.25, 0.50, 0.75] with sine.inOut drift tweens on (x, alpha) over 4-8s with [0, 2000]ms per-shaft random initial stagger + 2-stop gradient at 0/1 + fixed RGB color fragment rgb(255,210,140)). label='Spotlights' for all 3. Disco has NO spotlight anchor — floor-flash gradient is overlay-class (Stage A4.5). Honkytonk has NO spotlight anchor — neon-tint flicker is overlay-class (Stage A4.5). Ghost venues have no spotlight or 3D content. Position consistency: all 3 anchors have yaw_deg=NULL and pitch_deg=NULL (2D-canvas screen-space, not sphere-pinned) — satisfies db/032's type-agnostic `venue_anchors_position_consistency` CHECK constraint, exactly as db/035 audio + db/036 particle seeds did. Payloads incorporate the §1 amendment (commit `d50cbc9`): geometry.gradient_stops added to all 3 kinds (per-kind gradient stop configuration captured in payload, not hardcoded in the renderer); color fragment DROPPED for hue-driven kinds (swept-beam-2d, pulsed-laser) — per-item color comes from top-level `hues` array + shared geometry.sat_pct/lit_pct; light-shaft RETAINS the color fragment (single shared color); drift_speeds negative-first; first_pulse_offset_beats scalar added; geometry.pivot field added to swept-beam-2d for consistency with §1.4 / §1.5. All three verification queries from the file footer PASS against prod 2026-05-27 first run: Q1 (`SELECT count(*) FROM public.venue_anchors WHERE type = 'spotlight'`) returned `spotlight_count = 3`; Q2 (per-venue id/kind/label, ordered by venue_id) returned exactly the 3 expected rows (festival/pulsed-laser, speakeasy/light-shaft, stadium/swept-beam-2d) with `label='Spotlights'`; Q3 (payload schema spot-check) returned correct `kind_marker` per row (stadium hues_len=4, festival hues_len=6 bpm=128, speakeasy x_init_norm_len=3) and `gradient_stops present` for all 3. Verification mechanics note: Supabase SQL Editor auto-appends a `limit 100` clause that broke the original Q3 single-query form by injecting the limit inside a CASE-expression string literal; verification was completed by splitting Q3 into 4 sub-queries (3 per-kind kind_marker queries + 1 gradient_stops-presence query). Not a payload issue — strictly a verification-mechanics artifact. No anon/grant verification queries needed — db/037 creates no functions. **Companion surface changes (this same commit):** `shell/venue-renderers/spotlight.js` (new file — the spotlight anchor renderer per spec §3, self-contained with its own RAF + per-tween elapsed-time tracking + 4 pure-JS ease functions matching GSAP's naming verbatim [power1.inOut, power2.in, power3.out, sine.inOut]; three-kind dispatch with the advanceTween ordering fix from §9 step 3 review [item.active_tween cleared BEFORE on_complete invocation so a callback that re-arms the tween sticks]; ctx.canvas-only context handling — A4a accepts only "2d-canvas", unknown context or unknown kind warns and returns no-op stop handle); `admin-venues.html` (spotlight anchor authoring panel per §4 — per-kind form with kind selector swapping between swept-beam-2d / pulsed-laser / light-shaft sections; count:N per-item array fields as JSON textareas with inline length-vs-count validation surfacing via .array-length-mismatch class; bounded 2D-canvas preview surface with Play/Replay/Stop affordances; anchor-level dirty tracking via `state.spotlightAnchorDirty` Set with `markSpotlightAnchorDirty(anchorId)` helper that adds via Set semantics — diverges from §9 step 4 proposal's Map semantics, corrected mid-implementation when inspection of A3 source revealed `state.particleAnchorDirty = new Set()`; PERMIT multi-anchor rule per spec §4.5 / D2 — "+ Add" stays visible regardless of count, deliberate divergence from A2/A3 PREVENT; v2.138 → v2.139 version stamp; beforeunload + venueHasAnyDirty + discardAllForVenue extended to include spotlight anchor dirty state; loadAndRenderSpotlightPanel called from venue-selection callback alongside particle/audio); `karaoke/stage.html` (one new `<script type="module" src="../shell/venue-renderers/spotlight.js"></script>` tag — registration only, not a reader-path change per D8 and the A2 Check 12 / A3 Check 18 precedent; no version bump per the A2/A3 precedent for registration-only changes). **Stage A4a is AMBIENT_PROFILES-first per D8:** the 3 seeded spotlight anchors are dormant data; karaoke/stage.html continues reading from AMBIENT_PROFILES until Stage A7 (the read-path switch + AMBIENT_PROFILES retirement) promotes anchors to load-bearing. **Sub-staging:** A4a is the 2D-canvas-only sub-stage of A4 per planning chat 2026-05-27 (commit `c2de05f`'s Direction §7 update). A4b ships 3D spotlight builders (stadium 4 cone meshes + speakeasy 40 candle Points) + absorbed 3D particle paths (stadium 2000 phone-lights + speakeasy 60 sphere-mesh smoke) + new Three.js admin preview surface as a separate propose-pause cycle. **Deferred items A4a generates** (to be filed at A4a closeout per spec §7, NOT in this commit): A4b modulator synthesis decision (3D phone-lights bind crowd_brightness for parity with A3's 2D phone-lights); PERMIT multi-anchor UI evolution when A4b ships speakeasy's 2nd spotlight anchor (point-light candles alongside light-shaft); GSAP-equivalent motion accuracy verification gap (renderer ease functions reproduce GSAP semantics within numerical precision but A7's read-path switch is the first prod exercise — refinement may be needed if visible divergence surfaces). **Mid-implementation findings worth filing at closeout:** §9 step 4 proposal's `state.spotlightAnchorDirty = new Map()` was corrected to Set (matching A3 pattern); `markSpotlightAnchorDirty` helper added but A3 uses inline `state.particleAnchorDirty.add(...)` — both work, helper centralizes Save/Cancel button enable. |

## How to update

When applying a new migration to production:

1. Apply via Supabase SQL Editor (or `supabase db push` if/when CLI workflow lands).
2. Append a new row to the table above with `✅`, today's date (YYYY-MM-DD), the applier name, and any notes (e.g., commit hash where the file shipped to repo, or non-obvious context like "applied during incident response").
3. Commit the update alongside any session-log entry that claims the migration shipped. The two commits should not be temporally separated.

## How to verify a `❓ Verify` row

Run a check before relying on the migration's contents:

- **Functions:** `SELECT proname FROM pg_proc WHERE proname = 'rpc_xxx';` — empty result means not applied.
- **Tables/columns:** `\d+ table_name` in psql, or query `information_schema.columns WHERE table_name = 'xxx'`.
- **Triggers:** `SELECT tgname FROM pg_trigger WHERE tgname = 'xxx';`
- **Policies:** `SELECT polname FROM pg_policy WHERE polrelid = 'public.table_name'::regclass;`

If confirmed applied, update the row to `✅` with the verification date in Notes (e.g., "Verified live in pg_trigger 2026-05-15"). If confirmed missing, apply the migration and update the row.

## Why this exists

The Session 5 Part 3a.2 hardware verification on 2026-05-02 surfaced that `db/016_remove_participant.sql` had been committed to the repo at `05d2cae` and the closing log claimed it had shipped, but it had never actually been applied to production Supabase. The miss caused gate item 5 (Remove Player UI) to fail with a 404 on the missing RPC. Migration was applied manually mid-session.

This was the second instance of a migration committed-but-not-applied slipping through (the first was earlier in Session 5 but undocumented). Without tracking, a third slip is inevitable. This file is the cheapest viable mitigation; upgrade to a `pg_proc`-diff script or Supabase CLI workflow if checklist discipline doesn't hold.

See `docs/DEFERRED.md` "No tracking of which db/*.sql migrations have been applied to production" for the full bug entry.
